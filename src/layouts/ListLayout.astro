---
import BaseLayout from "@/layouts/BaseLayout.astro";
import SidebarProfile from "@/components/SidebarProfile.astro";
import StatsCard from "@/components/StatsCard.astro";

interface TaxItem {
  name: string;
  count: number;
  href: string;
}

interface Props {
  title: string;
  description?: string;
  stats?: Array<{ title: string; value: string | number; href?: string }>;
  tags?: TaxItem[];
  hideSidebar?: boolean;
}

const { title, description, stats = [], tags = [], hideSidebar = false } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <div class={`grid-main list-layout-shell ${hideSidebar ? "no-side" : ""}`}>
    <section class="list-main">
      <slot />
    </section>

    {
      !hideSidebar && (
        <aside class="list-side" data-tag-count={String(tags.length)}>
          <SidebarProfile />
          <div class="stats-grid">
            {stats.map((item) => <StatsCard title={item.title} value={item.value} href={item.href} />)}
          </div>
        </aside>
      )
    }
  </div>
</BaseLayout>

<style>
  .list-layout-shell {
    position: relative;
  }

  .list-main {
    display: grid;
    gap: var(--space-4);
  }
  .list-side {
    display: grid;
    gap: var(--space-4);
    align-content: start;
    position: sticky;
    top: 84px;
    height: fit-content;
  }
  .stats-grid {
    display: grid;
    gap: var(--space-2);
  }
  .no-side {
    grid-template-columns: minmax(0, 1fr);
  }
  @media (max-width: 1024px) {
    .list-side {
      position: static;
      top: auto;
    }
  }
</style>
