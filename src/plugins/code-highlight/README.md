# Code Highlight Plugin Modules

`resolve-config.ts` resolves the syntax highlight provider pipeline from
`src/site.config.ts` and returns Astro markdown/integration settings.

Supported providers:

- `expressive`
- `shiki`
- `prism`
- `rehype-pretty-code`
